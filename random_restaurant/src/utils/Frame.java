package utils;

import javax.swing.*;

public interface Frame {


    JFrame setBackgroundImg(JFrame jFrame);
    /**
     * �⺻ ������ ������ �����մϴ�.
     * @param jFrame
     * @return
     */
    JFrame setFrameSettings(JFrame jFrame);
}
